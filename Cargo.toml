[package]
name = "learn_rust"
version = "0.1.0"
authors = ["whoisfpc <wo_fpc@outlook.com>"]

[dependencies]
sdl2 = { version = "0.31.0", features = ["bundled", "static-link"] }
gl = { path = "lib/gl" }

[build-dependencies]
walkdir = "2.2.6"

[features]
gl_debug = ["gl/debug"]
